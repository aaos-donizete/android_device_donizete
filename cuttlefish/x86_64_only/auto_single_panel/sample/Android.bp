android_app {
    name: "donizete_sample_app",

    srcs: ["src/**/*.kt"],

    certificate: "platform",
    platform_apis: true,
    system_ext_specific: true,

    static_libs: [
        "androidx.appcompat_appcompat"
    ],
}
